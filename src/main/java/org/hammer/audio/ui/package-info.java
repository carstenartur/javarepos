/**
 * UI-facing helpers that translate audio-domain snapshots into pixel-space data.
 *
 * <p>This is the only platform package that knows about pixels and panel dimensions. All other
 * packages stay in normalized {@code float} audio space.
 */
package org.hammer.audio.ui;
