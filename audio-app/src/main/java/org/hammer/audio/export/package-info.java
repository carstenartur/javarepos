/**
 * Evidence export: combines screenshot, raw samples, spectrum, spectrogram, stereo-delay, diagnosis
 * and metadata into a single reproducible measurement bundle directory.
 */
package org.hammer.audio.export;
